# DynamoDB table name for movie view count statistics
DYNAMODB_TABLE_STATS=MovieStats

# AWS region where DynamoDB is deployed
AWS_REGION=us-east-1

# Port for the public WebSocket server (clients connect here via /ws)
PORT=8080

# Port for the internal HTTP server (health check + Lambda notify endpoint)
INTERNAL_PORT=8081
